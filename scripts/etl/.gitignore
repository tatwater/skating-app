# Scratch working dir for raw extracts + intermediate GeoJSON/NDJSON.
# The Geofabrik .pbf is large and rebuilt daily — never commit it (see README).
.scratch/

# Intermediate artifacts if run outside .scratch/
*.osm.pbf
*.osm.pbf.md5

# The permanent OSM snapshot archive. Unlike `.scratch/`, this is NOT disposable: it is what makes a
# corpus reproducible, and Geofabrik rebuilds `-latest` daily so a deleted extract is unrecoverable.
# Mirror it rather than removing it. See README §"Pinning the OSM snapshot".
.raw/

# Mirror config (holds no secrets, but stays local).
.env.local

# The permanent NHD geodatabase archive (N7). Same rule as `.raw/`, and more load-bearing: USGS
# retired NHD on 2023-10-01, so this is a final snapshot with no successor. Mirror it, never delete.
.raw-nhd/

# NHD mirror config (holds no secrets, but stays local).
.env.nhd.local

# The 3DHP archive (N7). `source/` holds an 11.9 GB CONUS download that is deliberately NOT mirrored
# and is deleted after a successful clip; `waterbody/` holds the Northeast clip that IS mirrored.
.raw-3dhp/
.env.3dhp.local

# The GNIS gazetteer archive (N7 / D105). Same rule as `.raw-nhd/` and more urgent than most: the
# staged URL carries NO vintage and is overwritten in place, so this directory is the only version
# record that exists. And a GNIS name decides admission, not just labelling — see `gnisArchive.ts`.
.raw-gnis/

# GNIS mirror config (holds no secrets, but stays local).
.env.gnis.local
