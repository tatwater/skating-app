node_modules
dist
.output
.nitro
.tanstack
.vinxi
.vercel

# Local env (see .env.example). Never commit real keys.
.env
.env.*
!.env.example

# NOTE: src/routeTree.gen.ts is intentionally committed — it's regenerated by the Start
# plugin on dev/build, but committing it keeps `tsc` (CI type-check) working without a
# prior build step.
