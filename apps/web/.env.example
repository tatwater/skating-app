# Copy to `.env` and fill in real values. `.env` is gitignored.
# VITE_* vars are exposed to the client bundle by Vite — safe only for PUBLIC keys.
# The Clerk keys below are read server-side by @clerk/tanstack-react-start (no VITE_ prefix).

# Clerk (D26). Publishable key is public; SECRET KEY must stay server-side only.
CLERK_PUBLISHABLE_KEY=pk_test_PLACEHOLDER
CLERK_SECRET_KEY=sk_test_PLACEHOLDER

# Convex deployment URL (D2) — e.g. https://your-deployment.convex.cloud
VITE_CONVEX_URL=https://placeholder.convex.cloud

# Basemap vector tiles (D6). Blank → the Protomaps hosted demo `.pmtiles` (dev only). Set to the
# self-hosted Vermont extract's Convex serving URL — build/host/wire steps in scripts/basemap.
# Deployment-specific (dev vs prod differ). Public.
VITE_PMTILES_URL=

# The whole-planet z0–6 overview archive that sits UNDER the regional one, so the map has an ocean
# and continents everywhere instead of ending in a straight line at the regional archive's edge.
# Same value as mobile's `EXPO_PUBLIC_WORLD_PMTILES_URL`. Blank = renders, but stops dead outside
# the region. Public.
VITE_WORLD_PMTILES_URL=

# Sentry DSN (D29) — client + server crash reporting. Use the DSN from a SEPARATE `skating-web`
# Sentry project (same org as mobile, distinct project — see README). Blank disables both.
# Not secret (it's shipped to the browser); one value drives the client and the SSR server.
VITE_SENTRY_DSN=

# Sentry build-time source-map upload (D29). Only needed where you build for production
# (i.e. set these on Vercel, not usually locally). Without SENTRY_AUTH_TOKEN the Vite plugin
# is skipped entirely, so builds still succeed — you just get unminified-only stack traces.
SENTRY_ORG=teagan-atwater
SENTRY_PROJECT=skating-web
SENTRY_AUTH_TOKEN=

# Bathymetric contours (A06b). A second .pmtiles archive, added to the style only while a lake's
# drawer is open. Blank = the layer never mounts, which is correct rather than degraded.
VITE_BATHYMETRY_PMTILES_URL=

# The freeze-up imagery archive (A06e). A BASE URL, not a file — the client composes frame and index
# addresses under it. Blank = no scrubber; the Tier 1 aerial reveal is unaffected.
VITE_IMAGERY_ARCHIVE_URL=
