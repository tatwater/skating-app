import { PlaceholderScreen } from '../../src/components/PlaceholderScreen'

// The center create-report action (D28); offline-capable report flow lands in Phase 2.
export default function ReportScreen() {
  return (
    <PlaceholderScreen
      title="＋ Report"
      subtitle="Create a peer ice report — offline-capable flow arrives in Phase 2."
    />
  )
}
