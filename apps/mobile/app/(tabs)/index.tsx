import { PlaceholderScreen } from '../../src/components/PlaceholderScreen'

// Map is the default tab (D20/D28); MapLibre renderer lands in Phase 2.
export default function MapScreen() {
  return (
    <PlaceholderScreen
      title="Map"
      subtitle="The wintery, water-focused map lands in Phase 2 (MapLibre)."
    />
  )
}
