{
  "name": "@skating/extraction",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Prose → structured report fields (Phase A10 §1 / D196): the engine-independent contract, the Extractor interface, and the Claude and Jev engines behind it.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "@skating/core": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.7",
    "fast-check": "^3.23.0",
    "vitest": "^3.2.7"
  }
}
